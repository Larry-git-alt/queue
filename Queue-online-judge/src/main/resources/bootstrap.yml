spring:
  cloud:
    nacos:
      config:
        server-addr: nacos:8848
        file-extension: yml
      discovery:
        server-addr: nacos:8848
  profiles:
    active: dev
  application:
    name: judge